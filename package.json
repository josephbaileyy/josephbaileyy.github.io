{
  "name": "josephbaileyy.github.io",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "npm run generate:about",
    "dev": "vite",
    "generate:about": "node scripts/generate-about.mjs",
    "build": "npm run generate:about && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run --exclude 'tests/e2e/**'",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@types/three": "^0.184.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "postprocessing": "^6.39.1",
    "three": "^0.184.0"
  }
}
